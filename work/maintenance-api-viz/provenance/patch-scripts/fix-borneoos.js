const fs = require("fs");
const FILE = "/Users/andrew/Documents/Codex/2026-06-08/yo-i-m-in-the-production-2/code/accelerator-provisioning-app/maintenance-api.html";
let s = fs.readFileSync(FILE, "utf8");
const o = "flip a config bit on the switch” (BorneoOS reads as Google's machine OS image for the program, by contrast with the FS QA image; the name appears only in this requirement”) made Sean go looking:";
const n = "flip a config bit on the switch”) — BorneoOS reads as Google's machine OS image for the program, by contrast with the FS QA image; the name appears only in this requirement — made Sean go looking:";
if (s.includes(o)) { s = s.replace(o, n); fs.writeFileSync(FILE, s); console.log("repaired"); }
else console.log("NOT FOUND");
const m = s.match(/<script>([\s\S]*)<\/script>/);
try { new Function(m[1]); console.log("JS parses OK"); } catch (e) { console.log("JS ERROR:", e.message); }
